---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: lurraca/concourse-docker

inputs:
- name: code
- name: postfacto-api

run:
  path: /usr/bin/run-things
  args: ['code/run-api-tests']

